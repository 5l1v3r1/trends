#!/bin/sh
#
# Getopts added at 2020 Feb 22
# Eksi and limit options added at 2020 Feb 5 22:40
#
# TODO Store history in sqlite with dates.

usage() {
	name=$(echo $0 | awk -F'/' '{ printf $NF}')
	>&2 echo "usage: $name [ -e ] [ -g ] [ -l limit ] [ -n countrycode ]";
}
colored() {
	colorcode=1;
	while IFS= read -r line; do
		[ $colorcode -lt 3 ] && tput bold;
		tput setaf $colorcode; printf " • $line\n"; tput sgr0;
		[ $colorcode -ge 3 ] && colorcode=15 || let colorcode++
	done
}

eksi=false
google=false
nation=GB
usecolor=false
limit=10

# Args
while getopts :egcl:n: args; do
	case $args in
		(e) eksi=true;;
		(c) usecolor=true;;
		(l) limit="$OPTARG";;
		(g) google=true;;
		(n) google=true; nation=`echo $OPTARG | tr [:lower:] [:upper:]`;;
		(h) usage; exit 0;;
		([?]) usage; exit 2;;
	esac
done
goourl=https://trends.google.com/trends/trendingsearches/daily/rss?geo=$nation
eksurl=https://eksisozluk.com

# Request & Results
if $eksi || ! $google; then
	file=/tmp/eksitrends.txt
	curl -s  $eksurl | 
		pup -p '.topic-list.partial > li a text{}' | paste -d "|"  - - |
		awk -F'|' '{ sub(/b/,"00",$2); print $2, ":", $1; }' | sort -nr > $file;
		cat $file | head -n $limit |
			if $usecolor; then colored; else cat /dev/stdin; fi
fi
if $google; then
	file=/tmp/googletrends.txt
	curl -s $goourl | grep -o '<title>.*<\/title>' |
		sed 's/^\s*<title>\(.*\)<\/title>/\1/;1d' > $file
	cat $file | head -n $limit |
		if $usecolor; then colored; else cat /dev/stdin; fi 
fi
