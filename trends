#!/bin/sh
#
# TODO Store history in sqlite with dates.

usagemessage='[ -egrc ] [ -l limit ] [ -n countrycode ]'
usage() {
	>&2 echo "usage: `basename $0` $usagemessage";
}
colored() {
	colorcode=1;
	while IFS= read -r line; do
		[ $colorcode -lt 3 ] && tput bold;
		tput setaf $colorcode; printf " • $line\n"; tput sgr0;
		[ $colorcode -ge 3 ] && colorcode=15 || let colorcode++
	done
}

limit=10
nation=gb
use_eksi=false
use_rate=false
use_color=false
use_google=false

# Args
while getopts :egrcl:n: args; do
	case $args in
		(e) use_eksi=true;;
		(r) use_rate=true;;
		(c) use_color=true;;
		(l) limit="$OPTARG";;
		(g) use_google=true;;
		(n) use_google=true; nation=`echo $OPTARG | tr [:upper:] [:lower:]`;;
		(h) usage; exit 0;;
		([?]) usage; exit 2;;
	esac
done

goourl=https://trends.google.com/trends/trendingsearches/daily/rss?geo=`\
	echo $nation | tr [:lower:] [:upper:]`
eksurl=https://eksisozluk.com

# Request & Results
if $use_eksi || ! $use_google; then
	file=/tmp/eksitrends.txt
	curl -s  $eksurl | 
		pup -p '.topic-list.partial > li a text{}' | paste -d "|"  - - |
		awk -F'|' '{ sub(/b/,"00",$2); print $2, $1; }' |
			sort -nr |
			if ! $use_rate; then cut -d' ' -f2-; else cat /dev/stdin; fi > $file;
		cat $file | head -n $limit |
			if $use_color; then colored; else cat /dev/stdin; fi
fi
if $use_google; then
	file=/tmp/googletrends.txt
	curl -s $goourl | grep -o '<title>.*<\/title>' |
		sed 's/^\s*<title>\(.*\)<\/title>/\1/;1d' > $file
	cat $file | head -n $limit |
		if $use_rate; then awk '{print NR, $0}'; else cat /dev/stdin; fi |
		if $use_color; then colored; else cat /dev/stdin; fi 
fi
